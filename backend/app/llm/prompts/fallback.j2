{% if reasons -%}
This application scored {{ decision.score }} out of 1000 and was placed in the {{ decision.band }} band{% if decision.decision %} ({{ decision.decision|lower|replace('_', ' ') }}){% endif %}. The factors that contributed most were: {% for r in reasons %}{{ r.reason|trim|trim('.')|lower }}{% if not loop.last %}; {% endif %}{% endfor %}.
{%- else -%}
This application scored {{ decision.score }} out of 1000 and was placed in the {{ decision.band }} band{% if decision.decision %} ({{ decision.decision|lower|replace('_', ' ') }}){% endif %}. No individual risk factors were recorded against it.
{%- endif %}
{%- if signals.known_fraud_2hop %} The entity graph recorded {{ signals.known_fraud_2hop }} confirmed-fraud application{{ 's' if signals.known_fraud_2hop > 1 else '' }} within 2 hops of it when it was decided.{% endif %}
 This summary is generated directly from the decision record.
