# Web / DB
fastapi==0.115.12
uvicorn[standard]==0.34.2
pydantic==2.11.4
pydantic-settings==2.9.1
SQLAlchemy==2.0.40
psycopg[binary]==3.2.7
alembic==1.15.2

# Tests (run inside the api container)
pytest==8.3.5
httpx==0.28.1
ruff==0.11.8

# Security
python-jose[cryptography]==3.4.0
bcrypt==4.3.0
slowapi==0.1.9

# ML runtime. Copied VERBATIM from ml/requirements.txt: artifacts are pickled by Lane B on these versions.
# --- [artifact-critical] — must match backend/requirements.txt exactly -------------------------
numpy==2.4.6
scipy==1.17.1
pandas==3.0.6
scikit-learn==1.9.1
lightgbm==4.7.0
shap==0.51.0
numba==0.67.0
llvmlite==0.49.0
joblib==1.6.0
cloudpickle==3.1.2
pyyaml==6.0.3
# --- end [artifact-critical] ---

# Drift detection (A5)
river==0.26.1

# Semantic layer (Lane B, app/semantic). torch is installed from the CPU index in the Dockerfile first.
torch==2.14.0
sentence-transformers==6.1.0
transformers==5.17.0
